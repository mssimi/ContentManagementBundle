<div id="{{ block.id|slice(1)|replace({'/' : '-'}) }}{{ idSuffix|default('') }}" class="{% if is_granted('ROLE_CMS') %}editable-block{% endif %}">
    {{ block.content|raw }}
    {% if is_granted('ROLE_CMS') %}<a class="editable-button" href="{{ path('mssimi_block_edit', {'id': block.id}) }}">Edit</a>{% endif %}
</div>